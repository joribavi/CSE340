<%- include('partials/header') %>

    <main>
        <h1><%= title %></h1>
          <ul style="list-style: none">  
            <% categories.forEach(category => { %>
                <li><%= category.category_details %></li>
            <% }); %>
          </ul>  
    </main>

 <%- include('partials/footer') %>

 